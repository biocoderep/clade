pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3,<1.6
dendropy>=5.0
tabulate>=0.9
pyyaml>=6.0

# firthlogist is optional coefficient refinement only (see
# pyproject.toml's [project.optional-dependencies] "firth" extra and
# stage1_association.py's _independent_fit) -- Stage 1's required
# computation is statsmodels.Logit and runs without it. Uncomment to
# install it too:
# firthlogist
